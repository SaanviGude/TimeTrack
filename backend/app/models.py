# Legacy models file - importing from new modular structure
from .models.user import User

# Keep this file for backward compatibility with existing imports
# New code should import directly from app.models.user, app.models.organization, etc.
